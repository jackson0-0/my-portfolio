[build]
  command = "npm run build"
  publish = "dist"

# React Router uses client-side routing, so every path needs to be
# served index.html and let the browser router take over, otherwise
# a hard refresh on a route like /projects/ipo-analyzer 404s.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
